class Bee extends Grub {
  
  constructor () {
  	super();			// if we dont give any parameters here, would it inherit everything and then we can just overwrite what we want ?
  	this.age = 5;
  	this.color = 'yellow';
  	this.job = 'Keep on growing';
  }


};
